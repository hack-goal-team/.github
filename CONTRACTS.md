# CONTRACTS — технические контракты потоков

Требования из `Sources/8. ДЖКХ.md`, сведённые с фактами из `Sources/dataset/`. Принятые решения — в [DECISIONS.md](DECISIONS.md), схема — в [ARCHITECTURE.md](ARCHITECTURE.md). Цифры в скобках — номера строк ТЗ.

## Сводка

| Поток | Направление | Транспорт | Формат | Частота | Объём (факт) | ТЗ |
|---|---|---|---|---|---|---|
| Измерения СМВУ | источник → мы | очередь сообщений (ADR-005) | 6 полей, CSV/JSON | near-real-time | ~170 тыс. событий/сутки | разд. 7 (305), 13 |
| История срабатываний | источник → мы | тот же поток, флаг `тревожное` | то же | то же | 0.24–0.64% потока | разд. 7 (309), 13 (539) |
| Реестры | источник → мы | REST pull по расписанию (ADR-013) | CSV/XLSX, JSON/XML | по расписанию | 11 485 каналов, 95 объектов | разд. 6, 10 |
| Метеоданные | источник → мы | открытое API, HTTPS (ADR-017) | JSON | по расписанию | малый | разд. 13 (545) |
| Аутентификация | мы → каталог | LDAP bind | — | на вход | ≥ 20 пользователей (423) | разд. 11 |
| Решения диспетчера | пользователь → мы | REST `POST` (ADR-021) | JSON | по событию | зависит от эксплуатации | разд. 12 |
| Выдача прогнозов | мы → пользователь | REST + SSE (ADR-020) | JSON | интерактив | — | разд. 10 |

Интеграции с системами Заказчика — **строго только чтение** (разд. 13). Обезличивание выполняется на стороне источника (разд. 7), на нашей стороне обфускации нет.

## Журнал измерений: схема и диалекты

| Поле | Тип | Пример |
|---|---|---|
| `ид_события` | bigint, **не уникален** | `4224123461` |
| `ид_канала_данных` | int | `196736` |
| `дата` | date, ISO | `2026-01-01` |
| `время` | time | `00:00:00` |
| `тревожное` | bool | `f` / `t` |
| `значение_датчика` | text, полиморфный | `0.01`, `28`, `Обнаружен дым`, `01.08.2026 07:50:59` |

Проверено по всем 51.7 млн строк файлов 2019 и 2026: ровно 6 полей в каждой строке, ни одной кавычки, BOM отсутствует. Рабочие выгрузки однородны. Единственное исключение — `журнал_событий_пример.csv` из приложения к ТЗ: там поля в кавычках и `false`/`true`.

Файлы **почти** отсортированы по дате: 4 450 нарушений возрастания из 30.7 млн в 2026 (0.014%) и 10 593 из 21.0 млн в 2019 (0.05%). На порядок опираться нельзя, дату надо разбирать.

`ид_события` повторяется: в 2019 — 1 491 дубль, и все проверенные оказались полными копиями строк. Диапазоны между годами не пересекаются (2019: 749.6 млн … 1.69 млрд; 2026: 3.28 … 4.48 млрд). Поведение при расхождении — ADR-012.

`значение_датчика` разбирается по `тип_датчика` из справочника: десятичное (газ `0.00`–`0.33`), целое (температура `13`–`36`), категориальное (`Норма`, `Неисправен`, `Обесточен`, `Затоплен`, `Обнаружен дым`, `Движения нет`), временное. `01.01.1970 03:00:00` — epoch-zero вместо null.

Отдельного поля типа инцидента нет, тип закодирован значением: пожар — `Обнаружен дым`, подтопление — `Затоплен`, газ — `Обнаружен газ`, проникновение — `Не замкнут`, `Обнаружено движение`, `Рычаг сдернут`, отказ — `Неисправен`, `Обесточен`, `Отключено устройство`, `Батарея разряжена`.

## Динамика объёма

| | 2019 | 2022 | 2024 | 2026 (H1) |
|---|---|---|---|---|
| Строк | 20 993 328 | 43 483 149 | 49 023 883 | 30 695 389 |
| Дней | 365 | 365 | 364 | 180 |
| Строк в сутки | 57 516 | 119 132 | 134 681 | 170 530 |
| Каналов | 5 622 | 10 722 | 10 348 | 11 447 |
| Тревожных | 0.639% | 0.307% | 0.235% | 0.637% |
| Тревог в сутки | 368 | 366 | 317 | 1 086 |

Стабильно ~21 тыс. строк на мегабайт; 15 184 МБ всего → **порядка 319 млн строк** за 2019–2026. Средняя интенсивность ~2 события в секунду.

## Реестры: схема

- `справочник_каналов_датчиков` — `ид_канала_данных`, `тип_инж_системы`, `тип_датчика`, `тег_инженерной_системы`, `название_датчика`. 11 485 записей. Пожарная охрана 5 701, Диспетчерский контроль 2 364, Охранная 2 211, Температурная 600, Газовая 529, Диагностическая 80. Датчиков дыма 4 337 — 38% парка.
- `справочник_объектов_диспетчер` — `ид_объект`, `иерархия_уровень`, `родитель`, `вид_объекта`, `диспетчерское_название_объекта`. 95 записей: 1 `district`, 16 второго уровня, 78 третьего; `controlHouse` 65, `guardObject` 29. Один `родитель` висячий.
- **Справочники содержат поля в кавычках с запятыми внутри** (`"Темп. ВШ ПК88,5"`) — в отличие от журналов. Отсюда ADR-016.

## Контракт прогноза

Требования разд. 6 и 10 — вероятность, временной горизонт, локация, классификация типа инцидента, история отработки. Поля `prediction_log`:

- идентификатор цели прогноза — **поле, а не зашитая сущность**: единица прогноза не определена (ADR-003)
- тип инцидента, вероятность, горизонт — **не менее 24 часов** (разд. 9)
- момент расчёта, версия модели
- результат отработки: решение диспетчера с причиной, присоединяется позже (ADR-021)

## Бюджеты задержки — их два

| Контур | Бюджет | Источник |
|---|---|---|
| Формирование прогноза (inference) | ≤ 300 сек | разд. 11 (415); в разд. 9 то же как «5 минут» (363) |
| Обработка потоковых данных | ≤ 300 сек | разд. 11 (421) |
| Чтение дашбордом из журнала прогнозов | < 1 сек | **наше требование**, ТЗ не нормирует |

Формулировка «для одного объекта» в разд. 9 и 11 — это требование к латентности, а не предписание считать прогноз на объект: на выбор единицы прогноза ТЗ ограничений не накладывает (ADR-002).

ТЗ нормирует только фоновый контур. Интерактивного инференса в системе нет — дашборд читает посчитанное (ADR-020), поэтому оптимизировать модель под отклик интерфейса не требуется.

## Прочие требования

- **Хранение:** PostgreSQL 12+ (разд. 11), разделение на оперативный контур, архив и аналитические витрины (разд. 13). Глубина архива ≥ 12 лет (539); фактически выдано 8 лет.
- **Безопасность:** TLS 1.2+, аутентификация через LDAP/AD, RBAC, журналирование всех действий пользователей (разд. 11, см. ADR-023).
- **Надёжность:** восстановление после сбоя ≤ 4 часов, автоматическое резервное копирование (разд. 11).
- **Совместимость:** серверная часть Linux, браузеры Chrome и Яндекс.Браузер (разд. 11).
- **Форматы обмена:** CSV, XLSX файлами; JSON, XML по API; геоданные GeoJSON и WKT (разд. 7).
- **Нагрузка:** не менее 20 одновременных пользователей без деградации (разд. 11, 423).

## Расхождения ТЗ и фактических данных

1. **Решений диспетчера в данных нет.** Разд. 12 называет их источником разметки для дообучения, но в журналах ровно шесть колонок во всех годах. Отсюда ADR-004 и ADR-021.
2. **Журнал ОДС не отделён от потока измерений.** Разд. 13 (539) разводит источники, фактически это один журнал с флагом `тревожное` (ADR-010, вопрос в Q&A).
3. **Приложение 1 названо «журналом сработок»** (297, 699), но таблица внутри — «Журнал записей со значениями с каналов данных датчиков» (701), и в примере лежат рядовые температуры с интервалом в минуту.
4. **Связи канала с объектом нет.** Каналы адресуются текстовым тегом (83 различных префикса), объекты — числовым `ид_объект` (95). Совпадений ноль, по части до дефиса 1 из 32.
5. **Реестр не покрывает историю.** 1 004 канала из 5 622, встречающихся в журнале 2019, отсутствуют в текущем справочнике — это 2 481 645 событий, 11.8% журнала за год. Отсюда ADR-014 и ADR-015.
6. **Координат нет вообще** — ни у каналов, ни у объектов, хотя разд. 7 требует GeoJSON и WKT, а разд. 10 — интерактивную карту. Влияет и на привязку метео (ADR-018).
7. **Периодичность не та, что подразумевает Приложение 1.** Пример показывает запись раз в минуту, фактически события пишутся по изменению состояния: медиана 2 события на канал в сутки. - и что, данные - источник истины
8. **«REST API для получения данных из внешних систем» (401)** сформулировано двусмысленно — наш публичный API или наш клиент к чужим системам. Трактуем как наш публичный, опираясь на разд. 14.
9. **Пороговое алармирование уже встроено в источник:** значения `Температура выше 40ºC` и `Температура ниже 3ºC` приходят готовыми. Rule-based бейзлайн существует на стороне СМВУ, модель должна его превосходить.